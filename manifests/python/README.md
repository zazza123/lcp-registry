# Python Manifests

This directory contains LCP manifests for Python packages.

## Structure

```
python/
└── {package_name[0]}/
    └── {package_name}/
        ├── {version}.lcp.json.gz
        └── latest.json
```

See the [registry README](../../README.md) for full contribution instructions.
